checkAny = function()
{
    return (up || left || down || right || confirm || cancel || cancelHeld || option || extra || start || select || lb || rb || lt || rt);
}

menu_input_reset()
lastDevice = -1
